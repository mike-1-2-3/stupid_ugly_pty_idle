
#ifndef SIM_GAME_UTIL
#define SIM_GAME_UTIL

#include "common.hpp"

void stringChopper(string& long_string, vector<string>& segments, uint line_width, uint prefix_chars=0); 
uint randRange(uint min, uint max);

#endif
